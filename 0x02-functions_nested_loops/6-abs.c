#include "main.h"

/**
 * main - function computes the absolute value of an integer
 *
 * Return: Always return 0
 */

int _abs(int)
{
	if (n > 0)
	{
		n = n * 1;
	}
	else if (n == 0)
	{
		n = 0;
	}
	else if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}
